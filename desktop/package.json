{
  "name": "forgechat-desktop",
  "version": "3.1.0",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:all": "cd ../client && npm run build && cd ../desktop && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
